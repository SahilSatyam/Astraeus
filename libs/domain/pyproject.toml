[project]
name = "astraeus-domain"
version = "0.1.0"
description = "Astraeus domain types and base errors. Pure Python, no IO."
requires-python = ">=3.14,<3.15"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["astraeus_domain"]
