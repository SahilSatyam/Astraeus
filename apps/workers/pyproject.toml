[project]
name = "astraeus-workers"
version = "0.1.0"
description = "Astraeus background workers: outbox relay, streaming, nightly jobs."
requires-python = ">=3.12,<3.13"
dependencies = [
    "astraeus-config",
    "astraeus-db",
    "astraeus-marketdata",
    "astraeus-observability",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["astraeus_workers"]
